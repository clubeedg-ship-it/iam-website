---
phase: 02-brand-and-homepage-repositioning
plan: 03
type: execute
wave: 2
depends_on: ["02-01", "02-02"]
files_modified: []
autonomous: false
requirements: [BRND-01, BRND-02, BRND-03, HOME-01, HOME-02]

must_haves:
  truths:
    - "Shell and NL partial content are identical for each of the 3 page families"
    - "No stale spellen/games terminology remains in content areas of any of the 9 files"
    - "Product image, FAQ, and stat counter changes render correctly in browser"
  artifacts: []
  key_links:
    - from: "index.html"
      to: "partials/index-nl.html"
      via: "content parity check"
      pattern: "Programma's Beschikbaar"
    - from: "prijzen.html"
      to: "partials/prijzen-nl.html"
      via: "content parity check"
      pattern: "Programma's"
    - from: "over-ons.html"
      to: "partials/over-ons-nl.html"
      via: "content parity check"
      pattern: "Programma's"
---

<objective>
Verify NL/EN sync and visual correctness after the copy changes in plans 01 and 02.

Purpose: Per D-16, a final sync pass confirms no drift was introduced between shell pages and their partials, and that all terminology replacements landed correctly without breaking layout or content.

Output: Verified sync state across all 9 files. Any drift or missed replacements fixed in-place.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-brand-and-homepage-repositioning/02-CONTEXT.md
@.planning/phases/02-brand-and-homepage-repositioning/02-RESEARCH.md
@.planning/phases/02-brand-and-homepage-repositioning/02-01-SUMMARY.md
@.planning/phases/02-brand-and-homepage-repositioning/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full terminology audit and shell/partial parity check across all 9 files</name>
  <files></files>
  <read_first>
    - index.html (scan for any remaining spellen/Spellen outside nav)
    - partials/index-nl.html (scan for any remaining spellen/Spellen — should be 0)
    - partials/index-en.html (scan for any remaining games/Games outside Game Editor)
    - prijzen.html (scan for remaining spellen outside nav)
    - partials/prijzen-nl.html (should be 0 spellen)
    - partials/prijzen-en.html (should be 0 games except Game Editor)
    - over-ons.html (scan for remaining spellen outside nav)
    - partials/over-ons-nl.html (should be 0 spellen)
    - partials/over-ons-en.html (should be 0 games except Game Development)
  </read_first>
  <action>
Per D-14, D-15, D-16: verify sync and completeness.

**Step 1 — Terminology audit:**
Run occurrence counts for all 9 files:
```bash
for f in index.html partials/index-nl.html partials/index-en.html prijzen.html partials/prijzen-nl.html partials/prijzen-en.html over-ons.html partials/over-ons-nl.html partials/over-ons-en.html; do
  echo "$f:"
  grep -n "spellen\|Spellen\|games\|Games" "$f"
done
```

Expected results for NL partials: 0 occurrences (partials have no nav links).
Expected results for EN partials: only "Game Editor" (index-en) or "Game Development" (over-ons-en) remain.
Expected results for shell files: only nav/footer link references remain (lines in header/footer area).

If any unexpected occurrences are found, fix them using the same replacement patterns from plans 01 and 02.

**Step 2 — Shell/partial parity check:**
For each page family, compare the NL content between shell and partial:
- Extract content sections from shell (between header and footer markers)
- Compare against the NL partial content
- Any differences in the edited sections indicate drift that must be fixed

For index family: verify stat counter, product card image block, product card description, software card description, and FAQ 4th item all match between index.html and partials/index-nl.html.

For prijzen family: verify all ~11 terminology replacements match between prijzen.html and partials/prijzen-nl.html.

For over-ons family: verify all ~5 terminology replacements match between over-ons.html and partials/over-ons-nl.html.

**Step 3 — Verify critical content:**
- Confirm FAQ answer text is verbatim per D-05 (EN) and D-06 (NL): grep for key phrases "interactieve zandbak een eigen selectie" (NL) and "interactive sandbox has its own selection" (EN)
- Confirm product image src is exactly `media/products/2in1/1.png` in all 3 index files
- Confirm `loading="lazy"` is present on the image tag
- Confirm no orphaned SVG markup remains

If any issues found, fix them directly. If no issues found, report clean.
  </action>
  <verify>
    <automated>cd /Users/ottogen/Projects/IAM/iam-website && echo "=== NL PARTIALS spellen count (all should be 0) ===" && grep -ci "spellen" partials/index-nl.html partials/prijzen-nl.html partials/over-ons-nl.html && echo "=== EN PARTIALS games count ===" && grep -cin "games" partials/index-en.html partials/prijzen-en.html partials/over-ons-en.html && echo "=== Image present ===" && grep -c "media/products/2in1/1.png" index.html partials/index-nl.html partials/index-en.html && echo "=== FAQ verbatim NL ===" && grep -c "interactieve zandbak een eigen selectie" index.html partials/index-nl.html && echo "=== FAQ verbatim EN ===" && grep -c "interactive sandbox has its own selection" partials/index-en.html</automated>
  </verify>
  <acceptance_criteria>
    - `grep -ci "spellen" partials/index-nl.html` returns 0
    - `grep -ci "spellen" partials/prijzen-nl.html` returns 0
    - `grep -ci "spellen" partials/over-ons-nl.html` returns 0
    - In EN partials, "games" only appears as part of "Game Editor" or "Game Development"
    - `grep -c "media/products/2in1/1.png" index.html` returns 1
    - `grep -c "media/products/2in1/1.png" partials/index-nl.html` returns 1
    - `grep -c "media/products/2in1/1.png" partials/index-en.html` returns 1
    - `grep -c "interactieve zandbak een eigen selectie" index.html` returns 1
    - `grep -c "interactieve zandbak een eigen selectie" partials/index-nl.html` returns 1
    - `grep -c "interactive sandbox has its own selection" partials/index-en.html` returns 1
    - Shell pages only have "spellen/Spellen" in nav/footer link areas, not in content sections
  </acceptance_criteria>
  <done>Full audit confirms all 9 files are correctly updated. No terminology drift. Shell/partial parity verified for all 3 page families.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of homepage changes</name>
  <files></files>
  <action>
Present the site to the user for visual verification. Start a local server if not already running:
```bash
npx serve . -l 3000
```
Then guide the user through the verification steps below.
  </action>
  <what-built>Homepage product image replaced (SVG to real photo), FAQ updated with approved text, stat counter and card descriptions changed from "spellen/games" to "programma's/programs". Same terminology applied to pricing and about pages.</what-built>
  <how-to-verify>
    1. Open the homepage in a browser (serve with `npx serve .` or equivalent)
    2. Scroll to the Products section — verify the 2-in-1 card shows a real photo of the white housing on casters, NOT a yellow SVG placeholder
    3. Verify the stat counter says "Programma's Beschikbaar" (not "Spellen Beschikbaar")
    4. Scroll to the FAQ section — verify the 4th question reads "Hoeveel interactieve programma's zijn er beschikbaar?"
    5. Expand the 4th FAQ — verify the answer mentions "interactieve zandbak" and ends with "Neem contact met ons op..."
    6. Switch to English (lang toggle) — verify FAQ says "How many interactive programs are available?" and answer mentions "interactive sandbox"
    7. Navigate to the pricing page — verify table headers and cells say "Programma's" / "Programs" (not "Spellen" / "Games")
    8. Navigate to the about page — verify "programma's" / "programs" appears in timeline, descriptions, and cards
  </how-to-verify>
  <verify>User confirms visual correctness</verify>
  <done>User has approved the visual presentation of all changes across homepage, pricing, and about pages.</done>
  <resume-signal>Type "approved" or describe any visual/content issues</resume-signal>
</task>

</tasks>

<verification>
Phase 2 complete when:
1. All 9 files updated with correct terminology
2. Homepage image, FAQ, and stat counter visually correct
3. No drift between shell and NL partials
4. Human has visually verified the changes
</verification>

<success_criteria>
- Visual confirmation that homepage looks correct with new image and FAQ (HOME-01, HOME-02)
- Terminology audit passes with zero unexpected "spellen/games" in content areas (BRND-03)
- Shell/partial parity confirmed for all 3 page families (per D-14, D-15, D-16)
- Human approval of the visual presentation (BRND-01, BRND-02)
</success_criteria>

<output>
After completion, create `.planning/phases/02-brand-and-homepage-repositioning/02-03-SUMMARY.md`
</output>
