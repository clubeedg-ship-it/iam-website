---
phase: 02-brand-and-homepage-repositioning
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - prijzen.html
  - partials/prijzen-nl.html
  - partials/prijzen-en.html
  - over-ons.html
  - partials/over-ons-nl.html
  - partials/over-ons-en.html
autonomous: true
requirements: [BRND-03]

must_haves:
  truths:
    - "Pricing page uses programma's/programs instead of spellen/games in all content areas"
    - "About page uses programma's/programs instead of spellen/games in all content areas"
    - "Shell and NL partial contain identical NL content for both page families"
    - "data-label attributes on pricing table cells match updated header text"
  artifacts:
    - path: "prijzen.html"
      provides: "Pricing shell with updated terminology"
      contains: "Programma's"
    - path: "partials/prijzen-nl.html"
      provides: "Pricing NL partial with updated terminology"
      contains: "Programma's"
    - path: "partials/prijzen-en.html"
      provides: "Pricing EN partial with updated terminology"
      contains: "Programs"
    - path: "over-ons.html"
      provides: "About shell with updated terminology"
      contains: "Programma's"
    - path: "partials/over-ons-nl.html"
      provides: "About NL partial with updated terminology"
      contains: "Programma's"
    - path: "partials/over-ons-en.html"
      provides: "About EN partial with updated terminology"
      contains: "Programs"
  key_links:
    - from: "prijzen.html"
      to: "partials/prijzen-nl.html"
      via: "content parity (shell NL == partial NL)"
      pattern: "Programma's"
    - from: "over-ons.html"
      to: "partials/over-ons-nl.html"
      via: "content parity (shell NL == partial NL)"
      pattern: "Programma's"
---

<objective>
Replace "spellen/games" with "programma's/programs" across the pricing and about page families.

Purpose: BRND-03 requires visitors to see current value language ("programs" not "games") on primary selling pages. The pricing page has ~11 occurrences per file and the about page has ~5 occurrences per file.

Output: Six updated files (3 per page family) with consistent terminology. No nav links or product names changed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-brand-and-homepage-repositioning/02-CONTEXT.md
@.planning/phases/02-brand-and-homepage-repositioning/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace spellen/games terminology on prijzen page family</name>
  <files>prijzen.html, partials/prijzen-nl.html, partials/prijzen-en.html</files>
  <read_first>
    - prijzen.html (full file — identify all "spellen/Spellen" occurrences with line numbers)
    - partials/prijzen-nl.html (full file — identify all "spellen/Spellen" occurrences)
    - partials/prijzen-en.html (full file — identify all "games/Games" occurrences)
    - .planning/phases/02-brand-and-homepage-repositioning/02-RESEARCH.md (prijzen edit inventory section)
  </read_first>
  <action>
Per D-11: replace ~11 "spellen/games" occurrences per file in the prijzen family.

**Categories to replace (per research edit inventory):**

1. **Table header** (1 per file):
   - NL: `<th>` containing `Spellen` -> `Programma's`
   - EN: `<th>` containing `Games` -> `Programs`

2. **Table cell data-labels** (6 per file):
   - NL: `data-label="Spellen"` -> `data-label="Programma's"`
   - EN: `data-label="Games"` -> `data-label="Programs"`
   - CRITICAL: These must match the updated `<th>` text for responsive table display (per Pitfall 5 in research)

3. **Table cell values** (4 per file):
   - NL: `60+ spellen` -> `60+ programma's`, standalone `spellen` -> `programma's`
   - EN: `60+ games` -> `60+ programs`, standalone `games` -> `programs`

4. **Package feature text** (3 per file):
   - NL: `interactieve spellen` -> `interactieve programma's`
   - EN: `interactive games` -> `interactive programs`

5. **FAQ answer text** (2 per file):
   - NL: `extra spellen` -> `extra programma's`
   - EN: `extra games` -> `extra programs`

**Special case per research:** "Klim Games" / "Klim Spellen" in the climbing wall table row — change to "Klim Programma's" / "Climbing Programs" as part of the systematic replacement. The climbing wall page itself is Phase 5 scope, but the pricing table label should use current terminology.

**Anti-patterns:**
- Do NOT change nav/footer link text containing "Spellen" or "Games" (links to the "3D Spellen" page). Shell files have nav at lines ~46 and ~96 — never touch these. Partials have NO nav links.
- Do NOT change "Game Editor" — it is a product name.
- Apply all replacements within the page content area only.
- For shell files (prijzen.html), the content area starts after the `#page-wrapper` opening and nav. For partials, the entire file is content.
  </action>
  <verify>
    <automated>cd /Users/ottogen/Projects/IAM/iam-website && echo "=== Remaining spellen in NL partial (should be 0) ===" && grep -ci "spellen" partials/prijzen-nl.html && echo "=== Remaining games in EN partial (should be 0 or only Game Editor) ===" && grep -ni "games\|Games" partials/prijzen-en.html && echo "=== data-label consistency ===" && grep -c 'data-label="Programma' partials/prijzen-nl.html && grep -c 'data-label="Programs"' partials/prijzen-en.html && echo "=== Programma count in NL partial ===" && grep -ci "programma" partials/prijzen-nl.html</automated>
  </verify>
  <acceptance_criteria>
    - `grep -ci "spellen" partials/prijzen-nl.html` returns 0 (partials have no nav, all replaced)
    - `grep -ci "games" partials/prijzen-en.html` returns 0 or only "Game Editor" matches
    - `grep -c 'data-label="Programma' partials/prijzen-nl.html` returns 6
    - `grep -c 'data-label="Programs"' partials/prijzen-en.html` returns 6
    - `grep -ci "programma" partials/prijzen-nl.html` returns at least 11
    - `grep -ci "programs" partials/prijzen-en.html` returns at least 11
    - In prijzen.html shell, only nav/footer link occurrences of "spellen/Spellen" remain (lines <120 or in footer section)
    - "Game Editor" references unchanged in all 3 files
  </acceptance_criteria>
  <done>All ~11 "spellen/games" occurrences per file replaced with "programma's/programs" across the prijzen page family. data-label attributes updated to match. Nav links untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Replace spellen/games terminology on over-ons page family</name>
  <files>over-ons.html, partials/over-ons-nl.html, partials/over-ons-en.html</files>
  <read_first>
    - over-ons.html (full file — identify all "spellen/Spellen" occurrences with line numbers)
    - partials/over-ons-nl.html (full file — identify all "spellen/Spellen" occurrences)
    - partials/over-ons-en.html (full file — identify all "games/Games" occurrences)
    - .planning/phases/02-brand-and-homepage-repositioning/02-RESEARCH.md (over-ons edit inventory section)
  </read_first>
  <action>
Per D-12: replace ~5 "spellen/games" occurrences per file in the over-ons family.

**Locations to replace (per research edit inventory):**

1. **Timeline item label:**
   - NL: `Interactieve Spellen` -> `Interactieve Programma's`
   - EN: `Interactive Games` -> `Interactive Programs`

2. **Game development description:**
   - NL: `spellen die zowel leuk als leerzaam` -> `programma's die zowel leuk als leerzaam`
   - EN: `games that are both fun` -> `programs that are both fun`

3. **Innovation paragraph:**
   - NL: `functies, spellen en toepassingen` -> `functies, programma's en toepassingen`
   - EN: `features, games, and applications` -> `features, programs, and applications`

4. **Passion paragraph:**
   - NL: `creativiteit van onze spellen` -> `creativiteit van onze programma's`
   - EN: `creativity of our games` -> `creativity of our programs`

5. **Updates card:**
   - NL: `nieuwe spellen` -> `nieuwe programma's`
   - EN: `new games` -> `new programs`

**Exception per research:** The "Game Development" section heading/label is a department discipline name. Do NOT change it. Similarly, "game development" in lowercase body text describing the team's discipline should stay as-is (Claude's discretion per research).

**Anti-patterns:**
- Do NOT change nav/footer link text containing "Spellen" or "Games". Shell files have nav links — never touch these. Partials have NO nav links.
- Do NOT change "Game Development" heading/label — it describes the team's discipline, not a product.
  </action>
  <verify>
    <automated>cd /Users/ottogen/Projects/IAM/iam-website && echo "=== Remaining spellen in NL partial ===" && grep -ni "spellen" partials/over-ons-nl.html && echo "=== Remaining games in EN partial ===" && grep -ni "games" partials/over-ons-en.html && echo "=== Programma count in NL partial ===" && grep -ci "programma" partials/over-ons-nl.html && echo "=== Programs count in EN partial ===" && grep -ci "programs" partials/over-ons-en.html</automated>
  </verify>
  <acceptance_criteria>
    - `grep -ci "spellen" partials/over-ons-nl.html` returns 0 (partials have no nav, all content occurrences replaced)
    - `grep -ci "programma" partials/over-ons-nl.html` returns at least 5
    - `grep -ci "programs" partials/over-ons-en.html` returns at least 5
    - In partials/over-ons-en.html, "games" only remains if part of "Game Development" (proper noun)
    - In over-ons.html shell, only nav/footer link occurrences of "spellen/Spellen" remain
    - "Game Development" heading/label unchanged in all 3 files
  </acceptance_criteria>
  <done>All ~5 "spellen/games" content occurrences per file replaced with "programma's/programs" across the over-ons page family. "Game Development" label preserved. Nav links untouched.</done>
</task>

</tasks>

<verification>
After both tasks complete:

1. Prijzen NL partial: 0 "spellen" occurrences, 11+ "programma" occurrences
2. Prijzen EN partial: 0 "games" occurrences (except "Game Editor"), 11+ "programs" occurrences
3. Over-ons NL partial: 0 "spellen" occurrences, 5+ "programma" occurrences
4. Over-ons EN partial: 0 "games" content occurrences (except "Game Development"), 5+ "programs"
5. Shell files match their NL partials for all changed sections
6. Nav links in shell files untouched (still say "Spellen" / "Games" for 3D page link)
7. "Game Editor" and "Game Development" proper nouns preserved
8. data-label attributes on prijzen table cells match updated header text
</verification>

<success_criteria>
- Pricing and about pages use "programma's/programs" terminology consistently (BRND-03)
- All data-label attributes on pricing tables match updated headers
- No over-replacement in nav links or product names
- Shell and NL partial content matches for both page families
</success_criteria>

<output>
After completion, create `.planning/phases/02-brand-and-homepage-repositioning/02-02-SUMMARY.md`
</output>
