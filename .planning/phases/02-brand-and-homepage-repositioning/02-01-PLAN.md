---
phase: 02-brand-and-homepage-repositioning
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
  - partials/index-nl.html
  - partials/index-en.html
autonomous: true
requirements: [HOME-01, HOME-02, BRND-01, BRND-02]

must_haves:
  truths:
    - "Homepage products section shows a real photo of the 2-in-1 housing instead of a yellow SVG placeholder"
    - "Homepage FAQ 4th item asks about interactive programs (not games) and shows the approved answer"
    - "Homepage stat counter says Programma's Beschikbaar / Programs Available instead of Spellen/Games"
    - "Homepage product card descriptions say programma's/programs instead of spellen/games"
    - "Shell (index.html) and NL partial contain identical NL content after edits"
  artifacts:
    - path: "index.html"
      provides: "Homepage shell with updated NL content"
      contains: "media/products/2in1/1.png"
    - path: "partials/index-nl.html"
      provides: "NL partial with same updates as shell"
      contains: "media/products/2in1/1.png"
    - path: "partials/index-en.html"
      provides: "EN partial with English equivalents"
      contains: "media/products/2in1/1.png"
  key_links:
    - from: "index.html"
      to: "media/products/2in1/1.png"
      via: "img src attribute"
      pattern: 'src="media/products/2in1/1.png"'
    - from: "partials/index-nl.html"
      to: "media/products/2in1/1.png"
      via: "img src attribute"
      pattern: 'src="media/products/2in1/1.png"'
    - from: "partials/index-en.html"
      to: "media/products/2in1/1.png"
      via: "img src attribute"
      pattern: 'src="media/products/2in1/1.png"'
---

<objective>
Update the homepage (index family) with the approved product image, FAQ content, and terminology changes.

Purpose: The homepage is the highest-visibility page. Visitors must see the real 2-in-1 housing photo, the correct FAQ about interactive programs, and "programma's/programs" terminology instead of "spellen/games."

Output: Three updated files (index.html, partials/index-nl.html, partials/index-en.html) with image swap, FAQ replacement, and terminology corrections applied consistently.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-brand-and-homepage-repositioning/02-CONTEXT.md
@.planning/phases/02-brand-and-homepage-repositioning/02-RESEARCH.md
@.planning/phases/01-safe-update-surface/change-brief.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace SVG placeholder with product image and update stat counter + product card text on homepage</name>
  <files>index.html, partials/index-nl.html, partials/index-en.html</files>
  <read_first>
    - index.html (full file — identify exact line numbers for SVG block, stat counter, product card description, software card description)
    - partials/index-nl.html (full file — same content sections at different line offsets)
    - partials/index-en.html (full file — EN equivalents)
    - .planning/phases/02-brand-and-homepage-repositioning/02-RESEARCH.md (exact edit inventory with line numbers and replacement markup)
  </read_first>
  <action>
Apply four edits to each of the 3 index family files. Per D-01, D-02, D-03, D-08, D-09.

**Edit A — Stat counter label (per D-08):**
- In index.html: find `Spellen Beschikbaar` in the stat-label div (~line 151), replace with `Programma's Beschikbaar`
- In partials/index-nl.html: find `Spellen Beschikbaar` (~line 41), replace with `Programma's Beschikbaar`
- In partials/index-en.html: find `Games Available` (~line 41), replace with `Programs Available`

**Edit B — Product card image swap (per D-01, D-02, D-03):**
Replace the entire SVG placeholder container block. The block to remove starts with `<div style="padding: 2rem; display: flex; align-items: center; justify-content: center;">` and contains the SVG element (80x80), the "2-in-1" label, and "Vloer + Muur" text. Replace this entire block with:

For index.html and partials/index-nl.html:
```html
<div style="padding: 2rem; display: flex; align-items: center; justify-content: center;">
    <img src="media/products/2in1/1.png"
         alt="IAM 2-in-1 interactieve vloer en muur behuizing"
         style="max-width: 100%; height: auto; border-radius: 16px;"
         loading="lazy">
</div>
```

For partials/index-en.html:
```html
<div style="padding: 2rem; display: flex; align-items: center; justify-content: center;">
    <img src="media/products/2in1/1.png"
         alt="IAM 2-in-1 interactive floor and wall housing"
         style="max-width: 100%; height: auto; border-radius: 16px;"
         loading="lazy">
</div>
```

Lines in index.html: ~298-306. Lines in partials/index-nl.html: ~188-196. Lines in partials/index-en.html: ~187-195. Confirm exact lines by reading first.

**Edit C — Product card description text (per D-09):**
- In index.html (~line 293): replace `interactieve spellen` with `interactieve programma's`
- In partials/index-nl.html (~line 183): replace `interactieve spellen` with `interactieve programma's`
- In partials/index-en.html (~line 182): replace `interactive games` with `interactive programs`

**Edit D — Software card description (per D-09):**
- In index.html (~lines 366-367): replace `interactieve spellen` with `interactieve programma's`
- In partials/index-nl.html (~lines 256-257): replace `interactieve spellen` with `interactieve programma's`
- In partials/index-en.html (~line 253): replace `interactive games` with `interactive programs`

**CRITICAL anti-patterns per research:**
- Do NOT change "spellen/games/Spellen/Games" in nav links or footer links (these reference the "3D Spellen" page which is out of scope)
- Do NOT change "Game Editor" — it is a product name
- Only edit within `#page-wrapper` content area, never touch `<head>` or `<script>` sections
- Shell pages have nav links at lines ~46 and ~96 — NEVER touch these
- Partials have NO nav links, so all occurrences in partials are safe to replace
  </action>
  <verify>
    <automated>cd /Users/ottogen/Projects/IAM/iam-website && echo "=== SVG removed ===" && grep -c "svg.*80.*80\|rgba(254,186,4" index.html partials/index-nl.html partials/index-en.html && echo "=== Image present ===" && grep -c "media/products/2in1/1.png" index.html partials/index-nl.html partials/index-en.html && echo "=== Stat counter ===" && grep -c "Programma's Beschikbaar" index.html partials/index-nl.html && grep -c "Programs Available" partials/index-en.html && echo "=== Product card text ===" && grep -c "interactieve programma" index.html partials/index-nl.html && grep -c "interactive programs" partials/index-en.html</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "svg.*80.*80" index.html` returns 0 (SVG placeholder removed)
    - `grep -c "rgba(254,186,4" index.html` returns 0 (yellow background removed)
    - `grep -c 'media/products/2in1/1.png' index.html` returns 1
    - `grep -c 'media/products/2in1/1.png' partials/index-nl.html` returns 1
    - `grep -c 'media/products/2in1/1.png' partials/index-en.html` returns 1
    - `grep -c "Programma's Beschikbaar" index.html` returns 1
    - `grep -c "Programma's Beschikbaar" partials/index-nl.html` returns 1
    - `grep -c "Programs Available" partials/index-en.html` returns 1
    - `grep -c "interactieve programma" index.html` returns at least 2 (product card + software card)
    - `grep -c "interactive programs" partials/index-en.html` returns at least 2
    - No "spellen" or "Spellen" occurrences remain outside of nav/footer links in index.html
    - No "games" or "Games" occurrences remain outside of nav/footer links in partials/index-en.html (except "Game Editor" which is a product name)
  </acceptance_criteria>
  <done>Homepage stat counter, product card image, product card description, and software card description all updated across all 3 index family files. SVG placeholder fully removed. Real product photo shown.</done>
</task>

<task type="auto">
  <name>Task 2: Replace FAQ question and answer with approved text on homepage</name>
  <files>index.html, partials/index-nl.html, partials/index-en.html</files>
  <read_first>
    - index.html (lines 560-590 — FAQ section, specifically 4th FAQ item)
    - partials/index-nl.html (lines 450-475 — same FAQ section)
    - partials/index-en.html (lines 445-470 — EN FAQ section)
    - .planning/phases/02-brand-and-homepage-repositioning/02-CONTEXT.md (D-04 through D-07 for exact approved text)
  </read_first>
  <action>
Apply FAQ replacement per D-04, D-05, D-06, D-07.

**In index.html (~lines 573-579) and partials/index-nl.html (~lines 462-468):**
Find the 4th FAQ `<details>` item. The current question contains "Hoeveel spellen zijn er beschikbaar?" (or similar wording about "spellen"). Replace the entire `<details>` block with:

```html
<details class="accordion-item">
    <summary>Hoeveel interactieve programma's zijn er beschikbaar?</summary>
    <div class="accordion-content">
        Het aantal beschikbare programma's hangt af van het product en het gekozen pakket.
        Onze interactieve wand en vloer bieden een breed scala aan programma's, terwijl de
        interactieve zandbak een eigen selectie heeft. Neem contact met ons op voor een
        overzicht dat past bij uw situatie.
    </div>
</details>
```

**In partials/index-en.html (~lines 457-463):**
Find the 4th FAQ `<details>` item. The current question contains "How many games are available?" (or similar). Replace with:

```html
<details class="accordion-item">
    <summary>How many interactive programs are available?</summary>
    <div class="accordion-content">
        The number of available programs depends on the product and the selected package.
        Our interactive wall and floor offer a wide range of programs, while the interactive
        sandbox has its own selection. Please contact us for an overview that fits your
        situation.
    </div>
</details>
```

The FAQ answer text is verbatim from the approved change brief — do NOT paraphrase or modify.

Preserve surrounding `<details>` items (FAQ items 1-3 and 5+) exactly as-is.
  </action>
  <verify>
    <automated>cd /Users/ottogen/Projects/IAM/iam-website && echo "=== NL FAQ question ===" && grep -c "Hoeveel interactieve programma" index.html partials/index-nl.html && echo "=== EN FAQ question ===" && grep -c "How many interactive programs" partials/index-en.html && echo "=== NL FAQ answer keyword ===" && grep -c "interactieve zandbak een eigen selectie" index.html partials/index-nl.html && echo "=== EN FAQ answer keyword ===" && grep -c "interactive sandbox has its own selection" partials/index-en.html && echo "=== Old FAQ removed ===" && grep -c "spellen zijn er beschikbaar\|games are available" index.html partials/index-nl.html partials/index-en.html</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Hoeveel interactieve programma" index.html` returns 1
    - `grep -c "Hoeveel interactieve programma" partials/index-nl.html` returns 1
    - `grep -c "How many interactive programs" partials/index-en.html` returns 1
    - `grep -c "interactieve zandbak een eigen selectie" index.html` returns 1
    - `grep -c "interactive sandbox has its own selection" partials/index-en.html` returns 1
    - `grep -c "spellen zijn er beschikbaar" index.html` returns 0 (old NL question removed)
    - `grep -c "games are available" partials/index-en.html` returns 0 (old EN question removed)
    - `grep "bewegingsspellen" index.html partials/index-nl.html` returns no matches (old compound word removed with old answer)
    - FAQ answer text matches approved wording verbatim (per D-05 and D-06)
  </acceptance_criteria>
  <done>Homepage FAQ 4th item updated with approved question and answer text in NL (shell + NL partial) and EN (EN partial). Old "spellen/games" FAQ content fully replaced.</done>
</task>

</tasks>

<verification>
After both tasks complete:

1. SVG placeholder fully removed — no `rgba(254,186,4` or `svg.*80.*80` in product card area
2. Real product image referenced — `media/products/2in1/1.png` appears in all 3 files
3. Stat counter updated — "Programma's Beschikbaar" (NL) / "Programs Available" (EN)
4. Product card + software card descriptions use "programma's" / "programs"
5. FAQ uses approved question and answer verbatim
6. Shell (index.html) and NL partial content match for all changed sections
7. Nav links and "Game Editor" references are untouched
8. `grep -c "spellen\|Spellen" partials/index-nl.html` returns 0 (partials have no nav, so all should be replaced)
9. `grep -c "games\|Games" partials/index-en.html` returns only "Game Editor" occurrences (1 expected)
</verification>

<success_criteria>
- Homepage shows real 2-in-1 housing photo instead of yellow SVG placeholder (HOME-01)
- Homepage FAQ asks about interactive programs with approved answer (HOME-02)
- Homepage uses "programma's/programs" terminology consistently (BRND-01, BRND-03)
- Real product photo replaces generic icon, accurately showing the housing (BRND-02)
- All changes applied identically to shell and NL partial; EN partial has English equivalents
</success_criteria>

<output>
After completion, create `.planning/phases/02-brand-and-homepage-repositioning/02-01-SUMMARY.md`
</output>
