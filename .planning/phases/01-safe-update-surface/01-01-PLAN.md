---
phase: 01-safe-update-surface
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-safe-update-surface/inventory.md
  - .planning/phases/01-safe-update-surface/change-brief.md
autonomous: true
requirements:
  - QLTY-01
  - QLTY-02

must_haves:
  truths:
    - "Every HTML file in the repo appears in exactly one file-family row in the inventory"
    - "Each file-family row shows shell path, NL partial path, EN partial path, and a drift flag"
    - "Orphaned partials are flagged separately as cleanup candidates"
    - "The spreadsheet change brief is available as structured markdown with actionable task rows and a package definition block"
  artifacts:
    - path: ".planning/phases/01-safe-update-surface/inventory.md"
      provides: "Complete file-family inventory with drift flags"
      contains: "| Family |"
    - path: ".planning/phases/01-safe-update-surface/change-brief.md"
      provides: "Parsed spreadsheet as structured markdown"
      contains: "## Actionable Tasks"
  key_links:
    - from: "inventory.md"
      to: "All 49 HTML files in the repo"
      via: "Every file listed in at least one family row"
      pattern: "index\\.html|prijzen\\.html|mobiele-vloer\\.html"
    - from: "change-brief.md"
      to: "website-adjustments.xlsx"
      via: "Parsed via pandas"
      pattern: "Homepage|IAM mobiel|solo|duo|premium"
---

<objective>
Build the canonical file-family inventory and parse the spreadsheet change brief into structured markdown.

Purpose: Downstream phases (2-5) need a single reference to know which files belong together, where drift has occurred, and what changes the business has requested. Without this, editors risk skipping files and introducing NL/EN drift.

Output:
- `inventory.md` -- complete file-family inventory with drift flags (per D-01, D-02, D-03)
- `change-brief.md` -- parsed spreadsheet with actionable tasks and package definition block (per D-10)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/CONCERNS.md
@.planning/phases/01-safe-update-surface/01-CONTEXT.md
@.planning/phases/01-safe-update-surface/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build file-family inventory with drift detection</name>
  <files>.planning/phases/01-safe-update-surface/inventory.md</files>
  <read_first>
    - index.html (inspect nav structure for drift comparison baseline)
    - partials/index-nl.html (compare nav against shell for drift detection method)
    - .planning/codebase/STRUCTURE.md (canonical file listing)
    - .planning/phases/01-safe-update-surface/01-CONTEXT.md (decisions D-01 through D-03)
    - .planning/phases/01-safe-update-surface/01-RESEARCH.md (verified codebase facts, drift detection method)
  </read_first>
  <action>
Create `.planning/phases/01-safe-update-surface/inventory.md` containing a complete file-family inventory of all 49 HTML files in the repo. Per D-01, inventory EVERY page family, not just spreadsheet-named ones.

**Structure the document with these sections:**

1. **## File Families** -- A markdown table with columns: `| Family | Shell | NL Partial | EN Partial | Drift? | Notes |`

   Populate from these known families (per D-02, each row has shell path, NL partial path, EN partial path, drift flag):

   Root families (8):
   - `index`: `index.html`, `partials/index-nl.html`, `partials/index-en.html`
   - `prijzen`: `prijzen.html`, `partials/prijzen-nl.html`, `partials/prijzen-en.html`
   - `over-ons`: `over-ons.html`, `partials/over-ons-nl.html`, `partials/over-ons-en.html`
   - `onderwijs`: `onderwijs.html`, `partials/onderwijs-nl.html`, `partials/onderwijs-en.html`
   - `parken-speelhallen`: `parken-speelhallen.html`, `partials/parken-speelhallen-nl.html`, `partials/parken-speelhallen-en.html`
   - `zorg-revalidatie`: `zorg-revalidatie.html`, `partials/zorg-revalidatie-nl.html`, `partials/zorg-revalidatie-en.html`
   - `3d-spellen`: `3d-spellen.html`, `partials/3d-spellen-nl.html`, `partials/3d-spellen-en.html`
   - `bouw-een-park`: `bouw-een-park.html`, `partials/bouw-een-park-nl.html`, `partials/bouw-een-park-en.html`

   Product families (7):
   - `interactieve-vloer`: `products/interactieve-vloer.html`, `partials/products/interactieve-vloer-nl.html`, `partials/products/interactieve-vloer-en.html`
   - `interactieve-muur`: `products/interactieve-muur.html`, `partials/products/interactieve-muur-nl.html`, `partials/products/interactieve-muur-en.html`
   - `interactieve-zandbak`: `products/interactieve-zandbak.html`, `partials/products/interactieve-zandbak-nl.html`, `partials/products/interactieve-zandbak-en.html`
   - `interactieve-klimwand`: `products/interactieve-klimwand.html`, `partials/products/interactieve-klimwand-nl.html`, `partials/products/interactieve-klimwand-en.html`
   - `interactieve-tekeningen`: `products/interactieve-tekeningen.html`, `partials/products/interactieve-tekeningen-nl.html`, `partials/products/interactieve-tekeningen-en.html`
   - `mobiele-vloer`: `products/mobiele-vloer.html`, `partials/products/mobiele-vloer-nl.html`, `partials/products/mobiele-vloer-en.html`
   - `software-maatwerk`: `products/software-maatwerk.html`, `partials/products/software-maatwerk-nl.html`, `partials/products/software-maatwerk-en.html`

   Standalone pages (no partials, 3):
   - `cookiebeleid`: `cookiebeleid.html`, none, none
   - `privacybeleid`: `privacybeleid.html`, none, none
   - `toegankelijkheid`: `toegankelijkheid.html`, none, none

   **Drift detection method:** For each family that has partials, compare the nav links in the shell page against the nav links in the NL partial using:
   ```bash
   diff <(grep -o 'href="[^"]*\.html[^"]*"' SHELL | sort) <(grep -o 'href="[^"]*\.html[^"]*"' NL_PARTIAL | sort)
   ```
   If diff output is non-empty, mark Drift = YES. If empty, mark Drift = NO. Record a brief note about what drifted (e.g., "nav link count differs", "footer links differ").

2. **## Orphaned Partials** -- Per D-03, list `partials/content-nl.html` and `partials/content-en.html` as cleanup candidates. Verify they are orphaned by running `grep -rn 'content-nl.html\|content-en.html' --include='*.html' .` (excluding `.planning/`) and confirming zero references from shell pages.

3. **## Removal Investigation Targets** -- Per D-08, flag these families:
   - `bouw-een-park` family (3 files) -- removal candidate
   - `3d-spellen` family (3 files) -- removal candidate
   - `interactieve-klimwand` family (3 files) -- spreadsheet row 46 says "Delete page", flagged for Phase 5 confirmation
   - "Choose Your Package" / "Kies Uw/Jouw Pakket" sections in product partials (12 files) -- section removal candidate

4. **## Summary Statistics** -- Total files: 49, families: 18 (15 with partials + 3 standalone), orphaned: 2, removal candidates: 3 families + 12 section removals.

5. **## File Checklist** -- A flat list of all 49 HTML files with a checkbox, so downstream phases can mark files as "updated" when they edit them. Generate by running `find . -name "*.html" -not -path "./.planning/*" | sort` and listing each.
  </action>
  <verify>
    <automated>bash -c 'FILE=".planning/phases/01-safe-update-surface/inventory.md" && test -f "$FILE" && echo "EXISTS" && grep -c "^|" "$FILE" | head -1 && grep -q "Orphaned" "$FILE" && echo "HAS_ORPHANED" && grep -q "Removal" "$FILE" && echo "HAS_REMOVAL" && grep -q "Drift" "$FILE" && echo "HAS_DRIFT"'</automated>
  </verify>
  <acceptance_criteria>
    - inventory.md exists at `.planning/phases/01-safe-update-surface/inventory.md`
    - inventory.md contains a markdown table with header `| Family |` and at least 18 data rows (15 families with partials + 3 standalone)
    - inventory.md contains `## Orphaned Partials` section listing `partials/content-nl.html` and `partials/content-en.html`
    - inventory.md contains `## Removal Investigation Targets` section listing `bouw-een-park`, `3d-spellen`, and `interactieve-klimwand`
    - Every family row has a Drift column value of YES or NO (not TBD, not empty)
    - inventory.md contains `## File Checklist` with 49 checkbox entries
  </acceptance_criteria>
  <done>All 49 HTML files accounted for in file-family rows or orphan/standalone sections. Every family has a concrete YES/NO drift flag. Removal candidates flagged per D-08.</done>
</task>

<task type="auto">
  <name>Task 2: Parse spreadsheet into structured change brief</name>
  <files>.planning/phases/01-safe-update-surface/change-brief.md</files>
  <read_first>
    - website-adjustments.xlsx (the source spreadsheet -- read to understand structure)
    - .planning/phases/01-safe-update-surface/01-RESEARCH.md (spreadsheet structure summary and parsing code example)
    - .planning/phases/01-safe-update-surface/01-CONTEXT.md (decision D-10)
  </read_first>
  <action>
Parse `website-adjustments.xlsx` into `.planning/phases/01-safe-update-surface/change-brief.md` per D-10. Use Python with pandas and openpyxl (both verified installed).

**Create a Python script inline or as a temp file that does:**

```python
import pandas as pd

xl = pd.ExcelFile('website-adjustments.xlsx')

# Sheet 1: "Website Adjustments"
df = pd.read_excel(xl, sheet_name='Website Adjustments')
# Structured task rows: rows where Section is not NaN
tasks = df.dropna(subset=['Section', 'Task'])
# Free-text block: rows where Page has content but Section is NaN (rows ~9-37)
notes = df[df['Section'].isna() & df['Page'].notna()]

# Sheet 2: "More info"
df2 = pd.read_excel(xl, sheet_name='More info')
```

**Structure the output markdown with these sections:**

1. **## Actionable Tasks** -- A table with columns: `| Row | Page | Section | Task | Current Situation | New Situation | Status |`
   Include all rows from Sheet 1 where Section and Task are non-NaN. Expected: approximately 10-12 structured task rows covering Homepage image, Homepage FAQ, Product page 2-in-1 text/image changes, Interactive floor deletions, Interactive wall changes, Sandbox/Climbing wall/Software section deletions.

2. **## IAM Mobiel Package Definitions** -- The free-text block from Sheet 1 rows 9-37 rendered as structured markdown. This contains the umbrella brand definition and three package tiers (solo, duo, premium) with pricing and positioning. Preserve the original wording since downstream phases need the exact approved language.

3. **## Detailed Instructions (More Info)** -- Content from Sheet 2 rendered as structured sections. This contains detailed instructions for Homepage products image replacement, FAQ question/answer replacement, 2-in-1 product page text and image changes, and full package specifications for floor-only, wall-only, and floor+wall configurations.

4. **## Cross-Reference to File Families** -- For each actionable task, map the "Page" column value to the corresponding file family name from the inventory. Example: "Homepage" maps to `index` family, "Product page 2-in-1" maps to `mobiele-vloer` family, "Interactive floor" maps to `interactieve-vloer` family, etc.

5. **## Open Questions** -- Flag any ambiguities found during parsing:
   - Row 46 "Delete page" for climbing wall vs D-08 only listing bouw-een-park and 3d-spellen
   - Any rows with unclear or empty target values
   - The "2-in-1" term appearing in spreadsheet but not found in current HTML (per Research pitfall 5)
  </action>
  <verify>
    <automated>bash -c 'FILE=".planning/phases/01-safe-update-surface/change-brief.md" && test -f "$FILE" && echo "EXISTS" && grep -q "## Actionable Tasks" "$FILE" && echo "HAS_TASKS" && grep -q "## IAM Mobiel Package Definitions" "$FILE" && echo "HAS_PACKAGES" && grep -q "## Detailed Instructions" "$FILE" && echo "HAS_DETAILS" && grep -q "solo" "$FILE" && echo "HAS_SOLO" && grep -q "duo" "$FILE" && echo "HAS_DUO" && grep -q "premium" "$FILE" && echo "HAS_PREMIUM"'</automated>
  </verify>
  <acceptance_criteria>
    - change-brief.md exists at `.planning/phases/01-safe-update-surface/change-brief.md`
    - change-brief.md contains `## Actionable Tasks` with a markdown table of at least 8 task rows
    - change-brief.md contains `## IAM Mobiel Package Definitions` with content referencing `solo`, `duo`, and `premium`
    - change-brief.md contains `## Detailed Instructions` with content from Sheet 2
    - change-brief.md contains `## Cross-Reference to File Families` mapping page names to family names
    - change-brief.md contains `## Open Questions` section
  </acceptance_criteria>
  <done>Spreadsheet fully parsed into structured markdown. All actionable tasks, package definitions, and detailed instructions captured. Downstream phases can consume this document without needing to re-read the xlsx file.</done>
</task>

</tasks>

<verification>
- `inventory.md` lists all 49 HTML files across 18 families + 2 orphans + 3 standalone
- Every family row has a concrete drift flag (YES/NO)
- `change-brief.md` contains all structured tasks from the spreadsheet
- Package definitions mention solo, duo, and premium tiers
- Both files are valid markdown that renders correctly
</verification>

<success_criteria>
- Complete file-family inventory exists with no HTML file unaccounted for
- Drift detection completed for all families with partials
- Spreadsheet parsed into machine-readable markdown with all four sections populated
- Removal candidates and open questions documented
</success_criteria>

<output>
After completion, create `.planning/phases/01-safe-update-surface/01-01-SUMMARY.md`
</output>
